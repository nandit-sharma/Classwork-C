// Accessing values using index value!
#include <stdio.h>

int main() {
    int sum=0, a, numbers[5]= {1, 2, 3, 4, 5};

// Printing the specific index.

    // printf("Enter the index: ");
    // scanf("%d", &a);
    // printf("Element %d = %d",a, numbers[a]);

// Printing the elements.

    // for(int i = 0;i<5;i++){
    //     printf("Element %d = %d\n",i, numbers[i]);
        
    // }

// Printing the sum of elements.

    // for(int i = 0;i<5;i++){
    //     sum += numbers[i];
        
    // }
    // printf("Sum = %d\n", sum);

    return 0;
}